import toggleSidebar from "./toggleSidebar";

const handleToggleSidebar = (
  currentTarget,
  sidebarToggled,
  setSidebarToggled
) => toggleSidebar(currentTarget, sidebarToggled, setSidebarToggled);

export default handleToggleSidebar;
